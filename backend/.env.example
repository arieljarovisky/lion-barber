PORT=4000

# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=lion_barber

# JWT para sesiones (cambiar en producción)
JWT_SECRET=tu-clave-secreta-muy-segura

# Google OAuth: Client ID de tu aplicación web (Google Cloud Console → APIs y servicios → Credenciales)
# Mismo valor que VITE_GOOGLE_CLIENT_ID en el frontend
GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com

# Admins: emails separados por coma que pueden acceder al dashboard (agenda, clientes, servicios…)
ADMIN_EMAIL=admin@ejemplo.com

# Super admins contables (opcional, además de los dos por defecto en código):
# agustincarluccio@gmail.com y jaroviskyariel@gmail.com — facturación AFIP, cierre de caja, estadísticas, monotributo
# SUPER_ADMIN_EMAILS=otro@ejemplo.com

# Mercado Pago: Access Token de la MISMA app y modo que el pago (TEST- = sandbox, APP_USR- = producción)
MERCADOPAGO_ACCESS_TOKEN=APP_USR-xxxxxxxx
FRONTEND_URL=http://localhost:3000
API_PUBLIC_URL=https://tu-backend-publico.com

# WhatsApp (Twilio): para avisar al barbero cuando se confirma una seña
# El número destino se toma de barbers.whatsapp_phone (por barbero, formato +54911...)
# TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_WHATSAPP_FROM=+14155238886

# Notificación gratis al celular de la barbería (Telegram Bot)
# Crear bot con @BotFather y obtener chat_id con @userinfobot o getUpdates
# TELEGRAM_BOT_TOKEN=123456789:ABCDEF...
# TELEGRAM_CHAT_ID=123456789

# Email transaccional al cliente. Hay 2 modos: Resend (recomendado en hostings como Railway) o SMTP.
# Prioridad: si RESEND_API_KEY está seteado se usa Resend, si no se intenta SMTP.

# --- Opción A: Resend (HTTPS, no usa SMTP, ideal cuando el hosting bloquea SMTP)
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# RESEND_FROM=onboarding@resend.dev   # mientras no tengas dominio verificado en Resend

# --- Opción B: SMTP (Gmail, Brevo, SendGrid, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false           # true para puerto 465
# SMTP_USER=tu-email@gmail.com
# SMTP_PASSWORD=app-password  # Para Gmail: contraseña de aplicación
# SMTP_FROM="Lion Barber <tu-email@gmail.com>"   # opcional, default usa SMTP_USER

# FRONTEND_URL=https://tu-sitio.vercel.app   # base del sitio (enlaces en emails: reprogramar, logo)
# SHOP_TIMEZONE=America/Argentina/Buenos_Aires   # hora local del turno para el recordatorio por mail ~1 h antes
# DISABLE_APPOINTMENT_REMINDER_EMAILS=1   # opcional: desactiva el job de recordatorio

# SHOP_NAME=Lion Barber       # opcional, aparece en los emails
# EMAIL_LOGO_URL=https://tu-dominio.com/lion-logo-hero-for-ui.png   # opcional. Si no se setea, se usa $FRONTEND_URL/lion-logo-hero-for-ui.png

# Afip SDK (facturación electrónica): https://app.afipsdk.com
# AFIP_PRODUCTION=false       # true en producción ARCA
#
# Cada barbero: token + CUIT + cert/clave en Configuración del panel (consumidor final, Factura C).
#
# Migración desde config vieja (solo Agustín / un barbero): al arrancar, si el barbero aún no tiene token en DB,
# se copian estas variables al barbero que coincida (nombre Agus/Agustin o AFIP_LEGACY_BARBER_ID=barber_1):
# AFIP_ACCESS_TOKEN=
# AFIP_CUIT=20387873709
# AFIP_PTO_VTA=1
# AFIP_CBTE_TIPO=11
# AFIP_CERT= / AFIP_KEY=  (PEM)  o  AFIP_CERT_PATH + AFIP_KEY_PATH
# AFIP_LEGACY_BARBER_ID=barber_1